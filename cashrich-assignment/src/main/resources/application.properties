server.port=8081

spring.datasource.url=jdbc:postgresql://localhost:5432/cashrich
spring.datasource.username=postgres
spring.datasource.password=sql
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

server.error.include-message=always

origin.key=23232-2323fdefe-3343f

# Allow any origin to access any URL
spring.webmvc.cors.allowed-origins=*
# Allow any method (GET, POST, PUT, DELETE, etc.)
spring.webmvc.cors.allowed-methods=*
# Allow any header
spring.webmvc.cors.allowed-headers=*